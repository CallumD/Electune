<% target = "song_row_#{song.id}" %>
<tr id="<%= target %>">
  <td><%= song.name %></td>
  <td><%= song.votes %></td>
  <td><%= render_upvote song, target %></td>
  <td><%= render_veto song, target %></td>
</tr>
