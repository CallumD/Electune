<tr>
  <td><%= link_to "lookup", album_lookup_path(playlist_id: @playlist._id, spotify_link: album.spotify_link), remote: true, class: "btn btn-primary loadonclick"  %></td>
  <td><%= truncate(album.name , :length => 40, :omission => '...') %></td>
  <td><%= link_to "album", album.spotify_link, class: "btn" %></td>
</tr>
